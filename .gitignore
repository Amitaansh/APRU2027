# Design source art — both files are over GitHub's 100 MB hard limit and are
# working files, not deliverables. Keep them in shared storage. The site ships
# the derivatives generated from them (codebase/public/images), and
# `npm run imagery` regenerates those from the originals.
DOA-APRU-Design-21Aug.ai
DOA-APRU-MainImage.jpg

# Scratch
_claude_tmp/

# Source documents stay local, not in the repo: the working-group proposals and
# the website sitemap carry personal email addresses for NUS and partner-
# university academics.
*.pdf
*.ai
APRU-SCL-2027-*.md
APRU-SCL-2027-DesignBrief-v2.html
apru-icon.jpg

# Committee portrait originals as downloaded from the DOA staff directory. Same
# rule as the design art above: sources stay local, the repo ships only the
# derivatives (codebase/public/images/committee), and `npm run imagery`
# regenerates those. See committee-source/SOURCE.md for the provenance table.
committee-source/
